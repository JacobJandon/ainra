{
  "name": "ainra",
  "version": "0.3.0",
  "description": "AINRA reference implementation — agent passports: issue, verify, revoke, log. Hybrid Ed25519 + ML-DSA-65; the download is one self-contained file (PQC bundled in), runs with just node.",
  "bin": { "ainra": "bin/ainra.js" },
  "engines": { "node": ">=18" },
  "license": "Apache-2.0",
  "devDependencies": {
    "@noble/post-quantum": "^0.2.0"
  }
}
