<!-- SPDX-License-Identifier: Apache-2.0 OR MIT -->
# Genesis Day rehearsal report

Generated by `make genesis-rehearsal` — the whole runbook in TEST MODE against staging. Everything labeled
"production" here is a **simulation** (prod-SIM on :8092, a SIM root); no ceremony, no 14-day clock, no
domains, no publishing. **10 phase(s) PASS, 0 FAIL.** Total 10s.

| # | Phase | Result | Time | Note |
|---|---|---|---|---|
| 1 | T-1d ceremony-dry-run (real dual TEST-ROOT ceremony, witness recomputes hash) | ✅ PASS | 3s | — |
| 2 | T-1d config-diff (production ≡ staging parity) | ✅ PASS | 0s | ✓ config-diff: production ≡ staging except {name, banner env, volumes, |
| 3 | T-1d staging health | ✅ PASS | 0s | — |
| 4 | T+0h mock cutover: prod-SIM artifact server boots + declares its network | ✅ PASS | 0s | — |
| 5 | T+0h banner flips by detection (AINRAscan logic: network=production → PRODUCTION display) | ✅ PASS | 0s | AINRAscan would show: PRODUCTION NETWORK |
| 6 | T+0h SDK verifies over the prod-SIM public artifacts | ✅ PASS | 1s | prod-SIM lineage verifies VALID in the SDK |
| 7 | T+0h witness health (an independent witness is reachable) | ✅ PASS | 0s | — |
| 8 | T+0h→T+14d soak stand-in (make soak-smoke — the instrument for the real 14-day clock) | ✅ PASS | 4s | — |
| 9 | T+14d declaration pipeline FAILS CLOSED on missing real artifacts (correct) | ✅ PASS | 1s | — |
| 10 | T+14d genesis board reads its honest count | ✅ PASS | 0s | — |

## What the rehearsal proves

- The ceremony choreography runs on this host (dry-run) and a witness recomputes the transcript hash.
- Production is config, not a fork: `config-diff` parity holds.
- The banner is **data-driven**: a server declaring `network=production` flips AINRAscan to PRODUCTION
  display, and the SDK still verifies every published lineage over the (simulated) production artifacts.
- The soak instrument works (`soak-smoke`) as the stand-in for the real 14-day clock.
- The declaration pipeline **fails closed**: with no real ceremony/soak evidence it refuses to render and
  lists **10** unproven claim(s) as TODOs — it can never overclaim by construction.
- `make genesis-status` reads the honest board: **7/11** (the 4 external rows stay ⏳ until real humans
  run them — this rehearsal advances the machinery, never the DoD).

## Friction found & runbook edits

- Clean run: no phase failed. (First run's friction, and the fix, are recorded in the milestone's
  DECISIONS/commit history — e.g. the staging-witness port move off the test range, and stage-smoke idempotency.)
