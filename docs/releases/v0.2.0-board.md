<!-- SPDX-License-Identifier: Apache-2.0 OR MIT -->
# v0.2.0 — preflight board evidence (M23.1)

The FULL preflight board, run from a **clean `git clone` at the release commit `0691f38`** — the proof
`docs/DECISIONS.md` D-040 and `RELEASING.md` require before v0.2.0 is tag-ready. Reproduce it yourself:
```sh
tmp="$(mktemp -d)"; git clone -q <repo> "$tmp/clone"
( cd "$tmp/clone" && git checkout -q 0691f38 && cargo fmt --all -- --check && make preflight )
```

```text
# AINRA v0.2.0 — full preflight board
# clean clone of /home/Ps3wb4n/Desktop/Solvatron/ainra at commit 0691f38
# host: Linux 7.1.3-zen1-3-zen x86_64 · rustc: cargo 1.96.1 (356927216 2026-06-26) · node: v26.4.0
# started: (timestamp intentionally omitted — reproducible evidence, not a log)

== HEAD: 0691f388484b1e27531a813edd114cd40a6faa4d ==
== fmt-clean under the pinned toolchain (rust-toolchain.toml) ==
fmt: 0 diffs ✓

== make preflight (full board — includes make repro) ==
bash tools/preflight.sh
AINRA preflight — clone-and-it-works board
toolchain: rustc 1.96.1 (31fca3adb 2026-06-26) · v26.4.0
────────────────────────────────────────────────────────────────
  … build + tests            [PASS] build + tests          release test suite (48s)
  … differential             [PASS] differential           3 impls agree over vectors (26s)
  … CLI hybrid               [PASS] CLI hybrid             download: Ed25519+ML-DSA both (2s)
  … genesis-local            [PASS] genesis-local          whole stack boots on 1 host (22s)
  … verifier kit             [PASS] verifier kit           execution-bound attestation (3s)
  … ceremony dry-run         [PASS] ceremony dry-run       witness-reproducible (11s)
  … ceremony multi           [PASS] ceremony multi         FROST 5-of-9 across processes (5s)
  … soak instrument          [PASS] soak instrument        measured p95, signed report (3s)
  … witness quorum           [PASS] witness quorum         fork refused over HTTP (6s)
  … S7 neutrality            [PASS] S7 neutrality          no brands / no impersonation (0s)
  … license headers          [PASS] license headers        SPDX on every source file (0s)
  … status honesty           [PASS] status honesty         README == STATUS claim (0s)
  … doc freeze               [PASS] doc freeze             normative docs unchanged (0s)
  … MCP fidelity             [PASS] MCP fidelity           ainra_verify ≡ SDK on vectors (3s)
  … presentation shape       [PASS] presentation shape     CLI≡middleware≡MCP event (15s)
  … skills replay            [PASS] skills replay          skills.md runs as written (4s)
  … reproducibility          [PASS] reproducibility        artifacts rebuild byte-exact (346s)
────────────────────────────────────────────────────────────────
  ALL GREEN — a stranger can clone this repo and every gate passes.

== BOARD EXIT: 0 ==
BOARD_DONE exit=0
```
