<!-- SPDX-License-Identifier: Apache-2.0 OR MIT -->
# v0.3.0 — preflight board evidence (M25)

The FULL preflight board, run from a **clean `git clone` at the release commit `75e65ba`** — the proof
`docs/DECISIONS.md` D-040 and `RELEASING.md` require before v0.3.0 is tag-ready. This board includes the
`conformance` row new in v0.3.0 (the self-serve runner: three real implementations clean over the full corpus,
a sabotaged one caught with named divergences) and the differential's fourth column (the independent Python
verifier, D-041). Reproduce it yourself:
```sh
tmp="$(mktemp -d)"; git clone -q <repo> "$tmp/clone"
( cd "$tmp/clone" && git checkout -q 75e65ba && cargo fmt --all -- --check && make preflight )
```

```text
# AINRA v0.3.0 — full preflight board
# clean clone of /home/Ps3wb4n/Desktop/Solvatron/ainra at commit 75e65ba
# host: Linux 7.1.3-zen1-3-zen x86_64 · rustc 1.96.1 (31fca3adb 2026-06-26) · node v26.5.0
# started: (timestamp intentionally omitted — reproducible evidence, not a log)

== HEAD: 75e65bac0d06f564de7b7593a3fad8341383462f ==
== fmt-clean under the pinned toolchain (rust-toolchain.toml) ==
fmt: 0 diffs ✓

== make preflight (full board — includes make repro + conformance) ==
bash tools/preflight.sh
AINRA preflight — clone-and-it-works board
toolchain: rustc 1.96.1 (31fca3adb 2026-06-26) · v26.5.0
────────────────────────────────────────────────────────────────
  … build + tests            [PASS] build + tests          release test suite (86s)
  … differential             [PASS] differential           4 impls agree over vectors (29s)
  … conformance              [PASS] conformance            runner: 3 clean, broken caught (33s)
  … CLI hybrid               [PASS] CLI hybrid             download: Ed25519+ML-DSA both (2s)
  … genesis-local            [PASS] genesis-local          whole stack boots on 1 host (27s)
  … verifier kit             [PASS] verifier kit           execution-bound attestation (4s)
  … ceremony dry-run         [PASS] ceremony dry-run       witness-reproducible (12s)
  … ceremony multi           [PASS] ceremony multi         FROST 5-of-9 across processes (6s)
  … soak instrument          [PASS] soak instrument        measured p95, signed report (3s)
  … witness quorum           [PASS] witness quorum         fork refused over HTTP (5s)
  … S7 neutrality            [PASS] S7 neutrality          no brands / no impersonation (1s)
  … license headers          [PASS] license headers        SPDX on every source file (0s)
  … status honesty           [PASS] status honesty         README == STATUS claim (0s)
  … doc freeze               [PASS] doc freeze             normative docs unchanged (0s)
  … MCP fidelity             [PASS] MCP fidelity           ainra_verify ≡ SDK on vectors (2s)
  … presentation shape       [PASS] presentation shape     CLI≡middleware≡MCP event (15s)
  … skills replay            [PASS] skills replay          skills.md runs as written (5s)
  … reproducibility          [PASS] reproducibility        artifacts rebuild byte-exact (354s)
────────────────────────────────────────────────────────────────
  ALL GREEN — a stranger can clone this repo and every gate passes.
== BOARD EXIT: 0 ==
BOARD_DONE
```

Note for the record: an earlier run of this identical board at parent `fd89ef1` also exited 0 on all rows, but
its differential row reported 41806s of wall-clock — the host suspended overnight mid-run (real compute is the
~29s above) — and carried the pre-fix "3 impls" row label. `75e65ba` differs from `fd89ef1` only by that
one-word label correction in `tools/preflight.sh`; the board above is the clean re-run at the corrected commit.
