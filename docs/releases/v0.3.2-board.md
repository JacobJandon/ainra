<!-- SPDX-License-Identifier: Apache-2.0 OR MIT -->
# v0.3.2 — the board at the release commit

`make preflight` from a **clean clone** at `680c9e5` ("packages: 0.3.1 → 0.3.2, and the CHANGELOG entry for
the operations release"), per [`RELEASING.md`](../../RELEASING.md) § *The one rule*. Run on 2026-08-08.

The published library source does not move in this release — `git diff v0.3.1..v0.3.2 -- packages/*/src crates/`
is empty. What moved is the surface around it, and the board is what proves the surface did not break the core.

```
bash tools/preflight.sh
AINRA preflight — clone-and-it-works board
toolchain: rustc 1.96.1 (31fca3adb 2026-06-26) · v26.5.0
────────────────────────────────────────────────────────────────
  … fmt + clippy             [PASS] fmt + clippy           rustfmt clean, no clippy warns (0s)
  … build + tests            [PASS] build + tests          release test suite (7s)
  … differential             [PASS] differential           4 impls agree over vectors (8s)
  … conformance              [PASS] conformance            runner: 3 clean, broken caught (35s)
  … CLI hybrid               [PASS] CLI hybrid             download: Ed25519+ML-DSA both (2s)
  … genesis-local            [PASS] genesis-local          whole stack boots on 1 host (26s)
  … verifier kit             [PASS] verifier kit           execution-bound attestation (3s)
  … ceremony dry-run         [PASS] ceremony dry-run       witness-reproducible (12s)
  … ceremony multi           [PASS] ceremony multi         FROST 5-of-9 across processes (0s)
  … soak instrument          [PASS] soak instrument        measured p95, signed report (4s)
  … witness quorum           [PASS] witness quorum         fork refused over HTTP (0s)
  … one decode path          [PASS] one decode path        no duplicate bytes→core parser (0s)
  … cross-impl interop       [PASS] cross-impl interop     fresh sigs verify in TS + PY (17s)
  … browser verifier         [PASS] browser verifier       745 vectors agree in-browser (3s)
  … S7 neutrality            [PASS] S7 neutrality          no brands / no impersonation (1s)
  … license headers          [PASS] license headers        SPDX on every source file (0s)
  … status honesty           [PASS] status honesty         README == STATUS claim (0s)
  … doc freeze               [PASS] doc freeze             normative docs unchanged (0s)
  … MCP fidelity             [PASS] MCP fidelity           ainra_verify ≡ SDK on vectors (3s)
  … presentation shape       [PASS] presentation shape     CLI≡middleware≡MCP event (16s)
  … skills replay            [PASS] skills replay          skills.md runs as written (4s)
  … reproducibility          [PASS] reproducibility        artifacts rebuild byte-exact (344s)
────────────────────────────────────────────────────────────────
  ALL GREEN — a stranger can clone this repo and every gate passes.
```
