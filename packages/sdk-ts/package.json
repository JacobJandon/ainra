{
  "name": "@ainra/sdk",
  "version": "0.1.0",
  "description": "AINRA verify-only SDK (TypeScript). Byte-matches the ainra-core canonical encoder; mirrors the nine verify steps. Independent implementation #2 for the differential harness.",
  "license": "Apache-2.0 OR MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@noble/post-quantum": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0"
  }
}
